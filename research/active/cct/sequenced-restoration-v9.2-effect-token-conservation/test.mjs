import test from"node:test";import assert from"node:assert/strict";import{assessEffectTokenConservation}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("accepts effects backed by unique minted admission tokens",()=>assert.equal(assessEffectTokenConservation(fullSetup()).status,"effect_token_conservation_candidate"));test("rejects a lateral effect without a minted admission token",()=>assert.deepEqual(assessEffectTokenConservation(fullSetup(true)).failures,["effect_without_admission_token"]));test("rejects replay of a minted token at an effect sink",()=>assert.deepEqual(assessEffectTokenConservation(fullSetup(false,true)).failures,["effect_token_reused"]));test("requires two independent effect-sink witnesses",()=>{const s=fullSetup();s.effectTokenReceipts=s.effectTokenReceipts.filter(x=>x.role!=="effect-sink"||x.observerId==="sa");assert.deepEqual(assessEffectTokenConservation(s).failures,["effect-sink_observer_quorum_missing"]);});
