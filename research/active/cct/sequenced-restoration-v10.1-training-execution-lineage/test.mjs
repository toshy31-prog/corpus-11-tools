import test from"node:test";import assert from"node:assert/strict";import{assessTrainingExecutionLineage}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("accepts training runs bound to runtime, data and configurations",()=>assert.equal(assessTrainingExecutionLineage(fullSetup()).status,"training_execution_lineage_candidate"));test("rejects a runtime configuration divergent from the manifest",()=>assert.deepEqual(assessTrainingExecutionLineage(fullSetup(true)).failures,["training_execution_diverged_from_manifest"]));test("requires two runtime observers",()=>{const s=fullSetup();s.trainingExecutionReceipts=s.trainingExecutionReceipts.slice(0,1);assert.deepEqual(assessTrainingExecutionLineage(s).failures,["training_runtime_observer_quorum_missing"]);});test("rejects an altered artifact receipt",()=>{const s=fullSetup();s.trainingExecutionReceipts[0].modelRuns[0].modelArtifactDigest="00".repeat(32);assert.deepEqual(assessTrainingExecutionLineage(s).failures,["training_execution_receipt_invalid"]);});
