import assert from"node:assert/strict";import{assessCommonInfluenceAttestation}from"../runtime.mjs";import{fullSetup}from"../fixtures.mjs";const r=assessCommonInfluenceAttestation(fullSetup(true));assert.equal(r.status,"common_influence_attestation_candidate");assert.deepEqual(r.warnings,["challenge_quorum_collapses_under_common_material_influence"]);console.log("held-out confrontation: shared funder collapses otherwise independent challenger quorum");
