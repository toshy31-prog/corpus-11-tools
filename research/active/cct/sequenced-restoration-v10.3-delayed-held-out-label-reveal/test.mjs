import test from"node:test";import assert from"node:assert/strict";import{assessDelayedHeldOutLabelReveal}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("accepts label reveal only after model artifacts are frozen",()=>assert.equal(assessDelayedHeldOutLabelReveal(fullSetup()).status,"delayed_held_out_label_reveal_candidate"));test("rejects label reveal before artifact freeze",()=>assert.deepEqual(assessDelayedHeldOutLabelReveal(fullSetup(true)).failures,["held_out_labels_revealed_before_artifact_freeze"]));test("requires two independent label custodians",()=>{const s=fullSetup();s.heldOutLabelRevealReceipts=s.heldOutLabelRevealReceipts.slice(0,1);assert.deepEqual(assessDelayedHeldOutLabelReveal(s).failures,["held_out_label_custody_quorum_missing"]);});test("rejects a changed artifact freeze",()=>{const s=fullSetup();s.modelArtifactFreeze.modelArtifactDigests[0]="0".repeat(64);assert.deepEqual(assessDelayedHeldOutLabelReveal(s).failures,["model_artifact_freeze_invalid"]);});
