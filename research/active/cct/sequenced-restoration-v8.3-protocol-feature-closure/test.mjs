import test from"node:test";import assert from"node:assert/strict";import{assessProtocolFeatureClosure}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("accepts complete matched protocol manifests",()=>assert.equal(assessProtocolFeatureClosure(fullSetup()).status,"protocol_feature_closure_candidate"));test("rejects a feature omitted from both manifests",()=>assert.deepEqual(assessProtocolFeatureClosure(fullSetup(true)).failures,["protocol_feature_catalog_incomplete"]));test("rejects an additional protocol difference",()=>{const s=fullSetup();s.shamProtocolFeatureManifest.instrumentation="extra-meter";assert.deepEqual(assessProtocolFeatureClosure(s).failures,["protocol_manifests_not_equivalent_except_treatment_assignment"]);});test("rejects an attestation not bound to the manifests",()=>{const s=fullSetup();s.protocolFeatureClosureAudits[0].realManifestDigest="00".repeat(32);assert.deepEqual(assessProtocolFeatureClosure(s).failures,["protocol_feature_closure_attestation_invalid"]);});
