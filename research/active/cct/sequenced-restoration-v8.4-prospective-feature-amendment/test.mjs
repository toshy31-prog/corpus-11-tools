import test from"node:test";import assert from"node:assert/strict";import{assessProspectiveFeatureAmendment}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("schedules a signed feature addition only for a future campaign",()=>{const r=assessProspectiveFeatureAmendment(fullSetup());assert.equal(r.status,"prospective_feature_amendment_candidate");assert.equal(r.effectiveFromCampaign,13);});test("rejects a retroactive amendment",()=>assert.deepEqual(assessProspectiveFeatureAmendment(fullSetup(true)).failures,["feature_amendment_not_prospective_or_invalid"]));test("rejects an amendment submitted after outcomes open",()=>{const s=fullSetup();s.protocolFeatureAmendment.submittedAtTick=70;assert.deepEqual(assessProspectiveFeatureAmendment(s).failures,["feature_amendment_not_prospective_or_invalid"]);});test("requires two failure-domain-separated challengers",()=>{const s=fullSetup();s.featureAmendmentAttestations=s.featureAmendmentAttestations.slice(0,1);assert.deepEqual(assessProspectiveFeatureAmendment(s).failures,["feature_amendment_quorum_missing"]);});
