import test from"node:test";import assert from"node:assert/strict";import{assessExecutedEdgeObservation}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("accepts signed observation that execution consumed the required edge",()=>assert.equal(assessExecutedEdgeObservation(fullSetup()).status,"executed_edge_observation_candidate"));test("rejects documentary compliance when execution ignores the edge",()=>assert.deepEqual(assessExecutedEdgeObservation(fullSetup(true)).failures,["required_edge_not_consumed_by_execution"]));test("requires two independent runtime observers",()=>{const s=fullSetup();s.executedLineageReceipts=s.executedLineageReceipts.slice(0,1);assert.deepEqual(assessExecutedEdgeObservation(s).failures,["executed_lineage_receipt_quorum_missing"]);});test("rejects observer disagreement",()=>{const s=fullSetup();s.executedLineageReceipts[1].executionDigest="00".repeat(32);assert.deepEqual(assessExecutedEdgeObservation(s).failures,["executed_lineage_receipt_invalid"]);});
