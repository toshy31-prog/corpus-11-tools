import assert from "node:assert/strict";import {assessPrecommittedValuationMethod} from "../runtime.mjs";import {fullSetup,signedValuationEndorsements,valuationMethod} from "../fixtures.mjs";const s=fullSetup();s.valuationMethod=valuationMethod(s.suspension,{registeredAtTick:s.suspension.openedAtTick+2,axes:s.valuationMethod.axes.map(x=>({...x,unit:"discounted-post-hoc-unit"}))});s.valuationEndorsements=signedValuationEndorsements(s.valuationMethod);const r=assessPrecommittedValuationMethod(s);assert.deepEqual(r.failures,["valuation_method_late_incomplete_or_unbounded"]);console.log("held-out confrontation: post-incident unit substitution is rejected despite valid signatures");
