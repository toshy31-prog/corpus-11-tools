import assert from"node:assert/strict";import{assessExternalObserverSeparationProtocol as assess}from"../runtime.mjs";import{fullSetup}from"../fixtures.mjs";const s=fullSetup();s.separationArtifacts.attempt+='tampered';const r=assess(s);assert.ok(r.failures.includes("separation_artifact_hash_mismatch"));console.log(JSON.stringify({ok:true,failure:"separation_artifact_hash_mismatch"}));
