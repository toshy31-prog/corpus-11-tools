import test from"node:test";import assert from"node:assert/strict";import{assessChallengerIndependenceLineage}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("accepts distinct audited control and influence lineages",()=>assert.equal(assessChallengerIndependenceLineage(fullSetup()).status,"challenger_independence_lineage_candidate"));test("rejects challengers under shared control",()=>assert.deepEqual(assessChallengerIndependenceLineage(fullSetup(true)).failures,["challengers_share_effective_control"]));test("requires two auditors per challenger",()=>{const s=fullSetup();s.challengerIndependenceAttestations=s.challengerIndependenceAttestations.filter(x=>x.auditorId==="ia");assert.deepEqual(assessChallengerIndependenceLineage(s).failures,["challenger_independence_audit_missing"]);});test("rejects a forged lineage attestation",()=>{const s=fullSetup();s.challengerIndependenceAttestations[0].ultimateControlDigest="00".repeat(32);assert.deepEqual(assessChallengerIndependenceLineage(s).failures,["challenger_independence_attestation_invalid"]);});
