import test from"node:test";import assert from"node:assert/strict";import{assessProspectiveEdgeChallenge}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("schedules an admitted omitted edge for the next campaign",()=>{const r=assessProspectiveEdgeChallenge(fullSetup());assert.equal(r.status,"prospective_edge_challenge_candidate");assert.equal(r.effectiveFromCampaign,14);});test("rejects retroactive edge insertion",()=>assert.deepEqual(assessProspectiveEdgeChallenge(fullSetup(true)).failures,["omitted_edge_challenge_not_prospective_or_invalid"]));test("requires two independent reviewers",()=>{const s=fullSetup();s.edgeChallengeAttestations=s.edgeChallengeAttestations.slice(0,1);assert.deepEqual(assessProspectiveEdgeChallenge(s).failures,["omitted_edge_challenge_quorum_missing"]);});test("rejects a forged challenge attestation",()=>{const s=fullSetup();s.edgeChallengeAttestations[0].challengeDigest="00".repeat(32);assert.deepEqual(assessProspectiveEdgeChallenge(s).failures,["omitted_edge_challenge_attestation_invalid"]);});
