import assert from"node:assert/strict";import{assessEnvelopeReconciliation}from"../runtime.mjs";import{fullSetup}from"../fixtures.mjs";const result=assessEnvelopeReconciliation(fullSetup(true));assert.deepEqual(result.failures,["admitted_envelope_without_unique_terminal"]);console.log("held-out confrontation: a traversed hidden branch leaves an externally observable unmatched envelope");
