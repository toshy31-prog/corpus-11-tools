import assert from"node:assert/strict";import{assessBoundedTrainingSearchLedger}from"../runtime.mjs";import{fullSetup}from"../fixtures.mjs";const result=assessBoundedTrainingSearchLedger(fullSetup(true));assert.deepEqual(result.failures,["unregistered_training_job_observed"]);console.log("held-out confrontation: an unpublished parallel model-search trial is detected by the compute ledger");
