import test from"node:test";import assert from"node:assert/strict";import{assessBoundedTrainingSearchLedger}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("accepts exactly the precommitted training jobs",()=>assert.equal(assessBoundedTrainingSearchLedger(fullSetup()).status,"bounded_training_search_ledger_candidate"));test("rejects an extra unregistered model-search trial",()=>assert.deepEqual(assessBoundedTrainingSearchLedger(fullSetup(true)).failures,["unregistered_training_job_observed"]));test("requires two compute meters",()=>{const s=fullSetup();s.trainingSearchMeterReceipts=s.trainingSearchMeterReceipts.slice(0,1);assert.deepEqual(assessBoundedTrainingSearchLedger(s).failures,["training_search_meter_quorum_missing"]);});test("rejects meter disagreement",()=>{const s=fullSetup();s.trainingSearchMeterReceipts[1].observedJobDigests=[];assert.deepEqual(assessBoundedTrainingSearchLedger(s).failures,["training_search_meter_receipt_invalid"]);});
