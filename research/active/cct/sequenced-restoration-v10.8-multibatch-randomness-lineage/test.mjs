import test from"node:test";import assert from"node:assert/strict";import{assessMultibatchRandomnessLineage}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("derives all four challenges from fresh per-batch contributions",()=>assert.equal(assessMultibatchRandomnessLineage(fullSetup()).status,"multibatch_randomness_lineage_candidate"));test("rejects seed reuse across batches",()=>assert.deepEqual(assessMultibatchRandomnessLineage(fullSetup(true)).failures,["multibatch_seed_reuse_or_order_invalid"]));test("requires two contribution domains",()=>{const s=fullSetup();s.multibatchRandomnessContributions=s.multibatchRandomnessContributions.slice(0,1);assert.deepEqual(assessMultibatchRandomnessLineage(s).failures,["multibatch_randomness_quorum_missing"]);});test("rejects a contribution changed after signing",()=>{const s=fullSetup();s.multibatchRandomnessContributions[0].batchContributions[3].seed="detached";assert.deepEqual(assessMultibatchRandomnessLineage(s).failures,["multibatch_randomness_receipt_invalid"]);});
