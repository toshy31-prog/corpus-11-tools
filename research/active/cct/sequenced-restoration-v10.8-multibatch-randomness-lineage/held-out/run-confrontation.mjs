import assert from"node:assert/strict";import{assessMultibatchRandomnessLineage}from"../runtime.mjs";import{fullSetup}from"../fixtures.mjs";const result=assessMultibatchRandomnessLineage(fullSetup(true));assert.deepEqual(result.failures,["multibatch_seed_reuse_or_order_invalid"]);console.log("held-out confrontation: a contributor cannot reuse one seed across nominally distinct batches");
