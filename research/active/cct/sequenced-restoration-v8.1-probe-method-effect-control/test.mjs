import test from"node:test";import assert from"node:assert/strict";import{assessProbeMethodEffectControl}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("accepts withdrawal evidence only when sham probes leave decisions unchanged",()=>{assert.equal(assessProbeMethodEffectControl(fullSetup()).status,"probe_method_effect_control_candidate");});test("rejects withdrawal evidence when sham probes reproduce the change",()=>{assert.deepEqual(assessProbeMethodEffectControl(fullSetup(true)).failures,["withdrawal_effect_confounded_by_probe_method"]);});test("requires two independent sham designs",()=>{const s=fullSetup();s.methodEffectAudits=s.methodEffectAudits.slice(0,1);assert.deepEqual(assessProbeMethodEffectControl(s).failures,["method_effect_audit_quorum_missing"]);});test("rejects a forged sham outcome",()=>{const s=fullSetup();s.methodEffectAudits[0].shamOutcomeDecisionDigest="aa".repeat(32);assert.deepEqual(assessProbeMethodEffectControl(s).failures,["method_effect_audit_invalid_or_nonindependent"]);});
