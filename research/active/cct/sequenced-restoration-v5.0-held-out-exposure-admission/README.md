# CCT 5.0 candidate — held-out exposure admission

Candidate 4.9 prevents retrospective registry amendments, but procedural cleanliness is not evidence that a proposed variable is useful. This candidate makes effective admission depend on a distinct future campaign whose predictions are frozen before outcome access.

The augmented predictor must improve paired Brier loss over the already registered baseline. Admission requires both a positive lower confidence bound above the declared minimum and enough precision to detect the declared material improvement. Validation provenance may not reuse an attestation source root, controller, or failure domain.

This remains a local research candidate using synthetic fixtures. It establishes an executable admission barrier, not construct validity, causal relevance, transport across campaigns, authorization, deployment, or external robustness.
