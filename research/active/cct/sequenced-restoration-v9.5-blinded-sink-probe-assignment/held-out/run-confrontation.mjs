import assert from"node:assert/strict";import{assessBlindedSinkProbeAssignment}from"../runtime.mjs";import{fullSetup}from"../fixtures.mjs";const result=assessBlindedSinkProbeAssignment(fullSetup(true));assert.deepEqual(result.failures,["sink_probe_blinding_audit_invalid"]);console.log("held-out confrontation: a probe-only visible header invalidates the sink enforcement evidence");
