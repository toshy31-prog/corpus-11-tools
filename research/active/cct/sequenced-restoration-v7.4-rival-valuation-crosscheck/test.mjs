import test from"node:test";import assert from"node:assert/strict";import{assessRivalValuationCrosscheck}from"./runtime.mjs";import{fullSetup,valuationCrosschecks}from"./fixtures.mjs";test("accepts independent rival measurements within the frozen tolerance",()=>assert.equal(assessRivalValuationCrosscheck(fullSetup()).status,"rival_valuation_crosscheck_candidate"));test("refuses materially divergent rival measurements",()=>{const s=fullSetup();s.valuationCrosschecks=valuationCrosschecks(s.valuationCrosschecks[0].valuationMethodDigest,{access:40,delay:10});assert.deepEqual(assessRivalValuationCrosscheck(s).failures,["rival_valuation_disagreement_exceeds_precommitment"]);});test("refuses reused measurement roots",()=>{const s=fullSetup();s.valuationCrosschecks[1].measurementRootDigest=s.valuationCrosschecks[0].measurementRootDigest;assert.deepEqual(assessRivalValuationCrosscheck(s).failures,["rival_valuation_independence_invalid"]);});test("allows a benign convention but refuses a shared material failure mode",()=>{const s=fullSetup();assert.equal(assessRivalValuationCrosscheck(s).status,"rival_valuation_crosscheck_candidate");s.valuationCrosschecks=valuationCrosschecks(s.valuationCrosschecks[0].valuationMethodDigest,undefined,true);assert.deepEqual(assessRivalValuationCrosscheck(s).failures,["rival_valuation_independence_invalid"]);});
