import assert from"node:assert/strict";import{assessEffectSinkRegistryClosure}from"../runtime.mjs";import{fullSetup}from"../fixtures.mjs";const result=assessEffectSinkRegistryClosure(fullSetup(false,true));assert.deepEqual(result.failures,["effect_sink_registry_incomplete"]);console.log("held-out confrontation: a sink found by both discovery methods cannot be omitted from the sealed registry");
