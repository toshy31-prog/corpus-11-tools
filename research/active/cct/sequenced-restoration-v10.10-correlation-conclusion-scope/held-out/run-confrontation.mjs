import assert from"node:assert/strict";import{assessCorrelationConclusionScope}from"../runtime.mjs";import{fullSetup}from"../fixtures.mjs";const result=assessCorrelationConclusionScope(fullSetup("statistical_independence_established"));assert.deepEqual(result.failures,["correlation_conclusion_exceeds_detectability"]);console.log("held-out confrontation: a clean bounded screen cannot be promoted to statistical independence");
