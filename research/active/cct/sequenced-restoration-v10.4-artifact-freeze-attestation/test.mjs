import test from"node:test";import assert from"node:assert/strict";import{assessArtifactFreezeAttestation}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("accepts two independent signed observations of the artifact freeze",()=>assert.equal(assessArtifactFreezeAttestation(fullSetup()).status,"artifact_freeze_attestation_candidate"));test("rejects an unsigned freeze claim",()=>{const s=fullSetup();delete s.artifactFreezeAttestations;assert.deepEqual(assessArtifactFreezeAttestation(s).failures,["artifact_freeze_attestation_missing"]);});test("requires two independent freeze observers",()=>{const s=fullSetup();s.artifactFreezeAttestations=s.artifactFreezeAttestations.slice(0,1);assert.deepEqual(assessArtifactFreezeAttestation(s).failures,["artifact_freeze_observer_quorum_missing"]);});test("rejects a forged observation time",()=>{const s=fullSetup();s.artifactFreezeAttestations[0].observedAtTick++;assert.deepEqual(assessArtifactFreezeAttestation(s).failures,["artifact_freeze_attestation_invalid"]);});
