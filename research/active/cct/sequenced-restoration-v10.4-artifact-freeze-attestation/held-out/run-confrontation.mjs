import assert from"node:assert/strict";import{assessArtifactFreezeAttestation}from"../runtime.mjs";import{fullSetup}from"../fixtures.mjs";const s=fullSetup();s.artifactFreezeAttestations=s.artifactFreezeAttestations.slice(0,1);const result=assessArtifactFreezeAttestation(s);assert.deepEqual(result.failures,["artifact_freeze_observer_quorum_missing"]);console.log("held-out confrontation: a freeze declared by only one observation domain is rejected");
