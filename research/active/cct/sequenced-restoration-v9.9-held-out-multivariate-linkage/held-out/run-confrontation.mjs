import assert from"node:assert/strict";import{assessHeldOutMultivariateLinkage}from"../runtime.mjs";import{fullSetup}from"../fixtures.mjs";const result=assessHeldOutMultivariateLinkage(fullSetup(true));assert.deepEqual(result.failures,["multivariate_probe_linkage_detected"]);assert.equal(result.modelResults.find(x=>x.modelFamily==="tree").auc,1);console.log("held-out confrontation: nonlinear linkage invisible to marginals is detected by the tree model");
