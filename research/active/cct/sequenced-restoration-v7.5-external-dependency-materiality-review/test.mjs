import test from"node:test";import assert from"node:assert/strict";import{assessExternalDependencyMaterialityReview}from"./runtime.mjs";import{fullSetup,materialityReviews}from"./fixtures.mjs";test("closes only after an external quorum agrees the shared dependency is nonmaterial",()=>{const r=assessExternalDependencyMaterialityReview(fullSetup());assert.equal(r.status,"external_dependency_materiality_review_candidate");assert.equal(r.reviewedSharedDependencies,1);});test("refuses producer self-classification without external reviews",()=>{const s=fullSetup();s.dependencyMaterialityReviews=[];assert.deepEqual(assessExternalDependencyMaterialityReview(s).failures,["external_materiality_review_quorum_missing"]);});test("keeps closure open when external reviewers disagree",()=>{const s=fullSetup();s.dependencyMaterialityReviews=materialityReviews(s.valuationCrosschecks,["nonmaterial","material"]);assert.deepEqual(assessExternalDependencyMaterialityReview(s).failures,["external_materiality_review_disagreement"]);});test("keeps closure open when the quorum finds material dependence",()=>{const s=fullSetup();s.dependencyMaterialityReviews=materialityReviews(s.valuationCrosschecks,["material","material"]);assert.deepEqual(assessExternalDependencyMaterialityReview(s).failures,["external_review_finds_shared_material_dependency"]);});
