import test from"node:test";import assert from"node:assert/strict";import{assessInfluenceEffectProbe}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("collapses a quorum only when withdrawal changes the decision",()=>{const r=assessInfluenceEffectProbe(fullSetup(true));assert.equal(r.status,"influence_effect_probe_candidate");assert.deepEqual(r.warnings,["challenge_quorum_collapses_under_transitive_common_influence"]);});test("refuses to infer coordination from a shared upstream resource without decision effect",()=>{assert.deepEqual(assessInfluenceEffectProbe(fullSetup(false)).failures,["common_influence_has_no_demonstrated_decision_effect"]);});test("requires two independent matched probes",()=>{const s=fullSetup();s.influenceEffectProbes=s.influenceEffectProbes.slice(0,1);assert.deepEqual(assessInfluenceEffectProbe(s).failures,["influence_effect_probe_quorum_missing"]);});test("rejects a forged outcome",()=>{const s=fullSetup();s.influenceEffectProbes[0].withdrawalDecisionDigest="ff".repeat(32);assert.deepEqual(assessInfluenceEffectProbe(s).failures,["influence_effect_probe_invalid_or_nonindependent"]);});
