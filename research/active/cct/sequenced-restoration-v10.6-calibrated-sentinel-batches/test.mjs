import test from"node:test";import assert from"node:assert/strict";import{assessCalibratedSentinelBatches,nullTailNumerator}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("derives the exact binomial null tail and family-wise bound",()=>{const r=assessCalibratedSentinelBatches(fullSetup());assert.equal(nullTailNumerator(),41449);assert.equal(r.status,"calibrated_sentinel_batches_candidate");assert.ok(r.familyWiseUpperBound<.001);});test("rejects a repeated batch presented as independent",()=>assert.deepEqual(assessCalibratedSentinelBatches(fullSetup(true)).failures,["sentinel_batch_set_invalid"]));test("rejects any batch crossing the precommitted threshold",()=>assert.deepEqual(assessCalibratedSentinelBatches(fullSetup(false,true)).failures,["sentinel_batch_rejection_threshold_crossed"]));test("rejects an optimistic family-wise numerator",()=>{const s=fullSetup();s.sentinelCalibrationManifest.familyWiseNumerator--;assert.deepEqual(assessCalibratedSentinelBatches(s).failures,["sentinel_calibration_manifest_invalid"]);});
