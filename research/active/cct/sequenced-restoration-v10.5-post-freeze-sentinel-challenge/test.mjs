import test from"node:test";import assert from"node:assert/strict";import{assessPostFreezeSentinelChallenge}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("accepts prediction commitments made before fresh sentinel targets",()=>assert.equal(assessPostFreezeSentinelChallenge(fullSetup()).status,"post_freeze_sentinel_challenge_candidate"));test("rejects near-exact agreement with targets generated after commitment",()=>assert.deepEqual(assessPostFreezeSentinelChallenge(fullSetup(true)).failures,["post_freeze_adaptation_signal"]));test("requires independent prediction and challenge quorums",()=>{const s=fullSetup();s.sentinelChallengeReceipts=s.sentinelChallengeReceipts.slice(0,1);assert.deepEqual(assessPostFreezeSentinelChallenge(s).failures,["sentinel_challenge_quorum_missing"]);});test("rejects targets generated before prediction commitment",()=>{const s=fullSetup();for(const x of s.sentinelChallengeReceipts)x.generatedAtTick=204;assert.deepEqual(assessPostFreezeSentinelChallenge(s).failures,["sentinel_challenge_receipt_invalid"]);});
