import assert from"node:assert/strict";import{assessPostFreezeSentinelChallenge}from"../runtime.mjs";import{fullSetup}from"../fixtures.mjs";const result=assessPostFreezeSentinelChallenge(fullSetup(true));assert.deepEqual(result.failures,["post_freeze_adaptation_signal"]);assert.equal(result.maximumSentinelMatches,32);console.log("held-out confrontation: predictions matching fresh post-commitment targets trigger withdrawal");
