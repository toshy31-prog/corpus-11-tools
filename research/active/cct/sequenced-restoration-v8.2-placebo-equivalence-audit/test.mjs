import test from"node:test";import assert from"node:assert/strict";import{assessPlaceboEquivalenceAudit}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("accepts placebo control only when treatment assignment is the sole difference",()=>{assert.equal(assessPlaceboEquivalenceAudit(fullSetup()).status,"placebo_equivalence_audit_candidate");});test("rejects a placebo with different observer visibility",()=>{assert.deepEqual(assessPlaceboEquivalenceAudit(fullSetup(false)).failures,["placebo_not_equivalent_except_treatment_assignment"]);});test("requires two independent equivalence auditors",()=>{const s=fullSetup();s.placeboEquivalenceAudits=s.placeboEquivalenceAudits.slice(0,1);assert.deepEqual(assessPlaceboEquivalenceAudit(s).failures,["placebo_equivalence_quorum_missing"]);});test("rejects a forged feature digest",()=>{const s=fullSetup();s.placeboEquivalenceAudits[0].invariantFeaturesDigest="ff".repeat(32);assert.deepEqual(assessPlaceboEquivalenceAudit(s).failures,["placebo_equivalence_invalid_or_nonindependent"]);});
