import test from"node:test";import assert from"node:assert/strict";import{assessBoundedCorrelationScreen,auditSentinelCorrelation}from"./runtime.mjs";import{fullSetup}from"./fixtures.mjs";test("accepts four derived batches within precommitted first-order bounds",()=>assert.equal(assessBoundedCorrelationScreen(fullSetup()).status,"bounded_correlation_screen_candidate"));test("rejects identical nominal batches",()=>{const v=Array.from({length:4},()=>Array.from({length:32},(_,i)=>i%2));assert.equal(auditSentinelCorrelation(v).failure,"sentinel_pairwise_distance_outside_bounds");});test("rejects a marginally degenerate batch",()=>{const v=Array.from({length:4},(_,j)=>Array.from({length:32},(_,i)=>(i+j)%2));v[3]=Array(32).fill(0);assert.equal(auditSentinelCorrelation(v).failure,"sentinel_marginal_balance_outside_bounds");});test("reports all six pairwise distances",()=>assert.equal(assessBoundedCorrelationScreen(fullSetup()).correlationAudit.distances.length,6));
