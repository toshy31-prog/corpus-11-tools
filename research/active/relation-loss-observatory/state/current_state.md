# État courant

La porte initiale reste une vérification `pipeline_verified`. Deux migrations
fictives appariées établissent maintenant, avec portée `model_internal`, que la
suppression d’une arête d’index retire l’accès et que la suppression d’une arête
de contexte retire la réutilisabilité ; restaurer l’arête restaure la sortie.

## Prochaine action interne utile

Varier séparément refus, permission, coût et compétence dans des graphes
fictifs afin de construire des mondes où le rival relationnel perd.
