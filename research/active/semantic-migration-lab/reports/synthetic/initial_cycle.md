# Résultat synthétique initial

- Exécution : `python3 tests/test_initial_protocol.py`
- Portée : `pipeline_verified`
- Résultat : un cas stable, une évolution de portée explicitement régie et une
  dérive d'attribution non expliquée sont distingués comme prévu.
- Les deux environnements sont des fonctions de test locales; aucune stabilité
  d'une version réelle de Corpus ou de ses dépendances n'est démontrée.

Le verdict est à retirer si une différence non déclarée est classée justifiée,
ou si les entrées comparées cessent d'être identiques.
