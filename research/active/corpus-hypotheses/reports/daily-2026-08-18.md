# Rapport quotidien — 2026-08-18

## Synthèse

La branche chiralité est close à l'ordre 3 ; factorisation a transporté son reste triple vers `S4` mais échoué au test prospectif H4 ; récupération/désinscription a ensuite reçu une réplication prospective `n=6`, un transport `asyncio`, puis un transport multi-processus local. La condition d'arrêt sur les bancs logiciels locaux est maintenant atteinte.

## Orientation compositionnelle

P1 `too_common`; P2 `not_transported` / `no_predictive_transport`; H3 `standard_absorption`; `I2+A2` suffit à supprimer les résidus P1/P2/joint. Aucun P3 adaptatif.

## Factorisation

Le test `S4` a trouvé une clé `(3,3,3)/(2,2,2)` compatible avec dimensions triples `1` et `2` (`transported_remainder`). H4 a ensuite testé 420 extensions par une quatrième matrice : quatre strates appariées, contrastes `+1,0,0,0`, médiane `0`, décision `not_supported`. La droite fixe commune `span((1,1,1,1))` explique le plancher de survie.

## Récupération / désinscription — réplication n=6

Famille prospective distincte du cas exploratoire : `32768` DAGs bruts, `9765` atteignables.

- `C_info=1` partout ;
- `C_erase_inf=1` partout ;
- `685` strates de contrôle ;
- `176` strates séparent `C_erase_1` ;
- distribution `C_erase_1` : `1:1`, `2:276`, `3:4824`, `4:4648`, `5:16` ;
- zéro violation de `C_erase_1=1+tau(G_int)`.

Décision : `replicated_profile_separation` ; classification bornée : `standard_profile_separation`.

## Transport runtime `asyncio`

Deux architectures appariées, budget de deux resets, 120 plans de latence répétés trois fois.

- A : `360/360` effacements ;
- B : `180/360` effacements, `180/360` résidus d'un bit ;
- `120` ordres runtime distincts par architecture ;
- zéro divergence modèle/runtime sur `720` runs.

Décision : `runtime_transport`.

La provenance brute des 720 runs est conservée en JSON gzip/base64 avec hashes SHA-256.

## Transport multi-processus local

Dernier banc logiciel local préenregistré : cinq processus OS persistants, état mémoire partagé synchronisé, temporisations `2,4,6,8,10 ms`, 120 permutations répétées deux fois par architecture.

### A

- `240/240` effacements ;
- `120` ordres réels distincts ;
- ordre réel = cible dans `231/240` ;
- zéro divergence modèle/runtime.

### B

- `120/240` effacements ;
- `120/240` échecs avec un bit résiduel ;
- `120` ordres réels distincts ;
- ordre réel = cible dans `233/240` ;
- zéro divergence modèle/runtime.

Décision : **`multiprocess_transport`**.

Le noyau a donc effectivement réordonné 16 runs au total, sans invalider la prédiction conditionnée par l'ordre observé.

## Lecture scientifique

L'énoncé opérationnel faible est maintenant robuste à trois niveaux : modèle exact, event loop réelle, processus OS séparés sur le même hôte.

Mais aucune nouveauté fondamentale n'est établie : dans la famille finie, le coût une-passe reste exactement `1 + couverture minimale de sommets`, et les deux transports logiciels concordent parfaitement avec le modèle discret.

## Statuts

- Récupération/désinscription : `active`, robustesse opérationnelle renforcée, mais profils standards ; aucun matériel.
- Factorisation : `active`, reste triple robuste ; stabilité prospective H4 non soutenue.
- Orientation compositionnelle : `weakened`, voie P1/P2 close.
- Frustration temporelle : inchangée, fortement bornée par l'ablation.
- Distinctions compatibles : suspendue.

## Condition d'arrêt et prochaine allocation

**Ne plus ajouter de banc logiciel local du même mécanisme.** Une nouvelle promotion de récupération/désinscription exige plusieurs machines, réseau externe, microcontrôleurs ou autre dispositif réellement distinct, avec le même contrat de mesure gelé.

Si ce dispositif n'est pas accessible, le prochain budget de recherche doit passer à la comparaison prospective de `F_T` contre des estimateurs standards dans une famille sans ordre latent commun, ou au quotient factoriel par le sous-espace fixe commun.
