# Rapport quotidien — 2026-08-18

## Changement substantiel

La branche d'orientation compositionnelle a subi quatre étapes gelées : rareté de la réponse forte (P1), transport hors protocole (P2), décomposition statique (H3), puis autopsie minimale de l'associateur. La voie P1/P2 est maintenant close à l'ordre 3.

## P1 — couplage par termes miroir

Observations exactes : `19 683` tables, `3 330` classes d'isomorphisme, `3 192` classes chirales, `1 690 / 3 192 = 52,94 %` fortement couplables. Décision : **`too_common`** ; la fenêtre préenregistrée était `1–25 %`.

## P2 — transport vers une sonde indépendante

La seconde sonde compare les distributions de rang de chaînes de translations gauche/droite aux horizons 2/3/4. `2 942 / 3 192` classes chirales répondent et 292 profils distincts existent, mais sur les 42 strates P1 gelées : 14 contrastes positifs, 11 nuls, 17 négatifs, médiane 0. Le contrôle prédictif donne 11 succès, 14 égalités, 17 échecs.

Décisions : **`not_transported`** et **`no_predictive_transport`**.

## H3 — décomposition statique P1/P2

Résidus exacts :

- `I0` : P1 `156`, P2 `148` ;
- `I1` : P1 `19`, P2 `14` ;
- `I2` : P1 `7`, P2 `2` ;
- `I3` : P1 `0`, P2 `0`, joint `0`.

Décision : **`standard_absorption`**.

## Autopsie minimale de l'associateur

Un audit séparé a gelé quatre raffinements de `I2` :

- `A1` : nombre de défauts associatifs ;
- `A2` : histogramme canonique des couples de sortie `((a*b)*c, a*(b*c))` ;
- `A3` : organisation par orbites de permutations `S3` des entrées ;
- `A4` : table canonique complète du défaut.

Résultats :

- `I2` : 1 582 cellules, résidus P1 `7`, P2 `2`, joint `7` ;
- `I2+A1` : identique ; `A1` est redondant car `I0` contient déjà le nombre de triplets associatifs ;
- **`I2+A1+A2` : 1 590 cellules, résidus P1 `0`, P2 `0`, joint `0`** ;
- `A3` porte le nombre de cellules à `1 592` sans modifier le zéro des résidus ;
- `A4` porte le nombre à `1 596`, soit toutes les paires `M/M^op`, mais n'est pas nécessaire pour déterminer P1/P2.

**Minimal sufficient refinement : `I2 + A2`.**

La structure sous orbites `S3` et la signature complète du défaut sont donc superflues pour expliquer les deux profils testés.

## Conclusion scientifique bornée

La formulation exacte retenue est :

> P1 et P2 sont déterminés, à l'ordre 3, par une compression statique formée des invariants de translations/semigroupes `I2` et de l'histogramme canonique des couples de sorties du défaut d'associativité.

Cela ne constitue pas un théorème sur toutes les dynamiques neutres imaginables.

### Orientation compositionnelle

Statut : **`weakened`** ; voie P1/P2 **close à l'ordre 3**.

- H1 : `too_common` ;
- H2 : `not_transported` ;
- H3 : `standard_absorption` ;
- absorbeur minimal testé : `I2 + A2`.

Ne sont pas établis : invariant intrinsèque de couplabilité, orientation macroscopique, amplification collective, canal matériel, émergence du temps, généralité hors ordre 3.

## Autres hypothèses

Aucun statut n'est modifié par cette clôture pour récupération/désinscription, frustration temporelle, invariants de factorisation, complexe de distinctions compatibles ou holonomie `S3`.

## Prochaine allocation de recherche

Ne pas lancer P3 ni une recherche adaptative d'ordre 4 sur cette branche.

La priorité passe aux hypothèses encore ouvertes. En premier : **invariants de factorisation**, parce que le reste d'intersection triple est exact, invariant sous changement de base et encore dépourvu de test prospectif sur des familles de transports non sélectionnées après résultat. En second : récupération/désinscription pour un test matériel/opérationnel dès qu'un canal expérimental est disponible.
