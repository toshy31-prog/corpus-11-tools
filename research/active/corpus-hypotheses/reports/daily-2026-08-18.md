# Rapport quotidien — 2026-08-18 — clôture

## Synthèse

Cycle exceptionnellement discriminant : quatre branches ont reçu des tests fermés plutôt que des prolongations narratives. Orientation compositionnelle P1/P2 est close à l'ordre 3 ; factorisation transporte son reste triple mais échoue à H4 ; récupération/désinscription transporte sa séparation jusqu'à cinq processus OS mais reste absorbée par un invariant standard ; enfin `F_T` perd directement contre Borda dans une population exhaustive sans ordre latent commun.

## Orientation compositionnelle

P1 `too_common`; P2 `not_transported` / `no_predictive_transport`; H3 `standard_absorption`; `I2+A2` suffit à déterminer P1/P2/joint. Aucun P3 adaptatif.

## Factorisation

Le test `S4` trouve une clé `(3,3,3)/(2,2,2)` compatible avec dimensions triples `1` et `2` (`transported_remainder`). H4 teste ensuite 420 extensions : quatre strates appariées, contrastes `+1,0,0,0`, médiane `0`, décision `not_supported`. La droite fixe commune `span((1,1,1,1))` explique le plancher de survie.

## Récupération / désinscription

Réplication n=6 : `32768` DAGs bruts, `9765` atteignables, `176` strates séparant `C_erase_1`, mais zéro violation de `C_erase_1=1+tau(G_int)`.

Transport `asyncio` : A `360/360`, B `180/360`, zéro mismatch sur `720` runs.

Transport cinq processus OS : A `240/240`, B `120/240`, zéro mismatch sur `480` runs ; 16 réordonnancements runtime sans rupture du modèle.

Conclusion : axes opérationnels distincts, mais mécanisme local entièrement standard. Condition d'arrêt : aucun nouveau banc logiciel local ; prochaine promotion seulement sur dispositif réellement distribué ou matériel.

## Frustration temporelle — test concurrent final du jour

Préenregistrement `temporal-borda-conditioned-preregistration-2026-08-18.md` gelé avant calcul.

Population : tous les `32768` tournois étiquetés à six sommets, groupés par vecteur étiqueté exact de degrés. Train et test sont distincts et partagent uniquement ce vecteur ; aucun ordre latent générateur n'est introduit.

- `2932` strates de degrés ;
- `2212` strates non triviales ;
- `1 343 184` couples train/test ordonnés ;
- perte F_T : moyenne exacte sur tous les ordres minimisant le train ;
- perte Borda : moyenne exacte sur tous les tie-breaks compatibles avec les degrés ;
- `Delta_total = L_Borda-L_FT = -472112` ;
- `Delta_mean ≈ -0,351487` violation/test ;
- F_T meilleur / égal / pire : `301248 / 370240 / 671696` ;
- avantage moyen F_T négatif dans **`2212/2212`** strates non triviales.

Décision préenregistrée : **`borda_better`**.

La condition de requalification inscrite dans `temporal-frustration.md` est atteinte. `F_T` reste un minimum mathématique exact d'arêtes de retour, mais n'a plus de pouvoir prédictif autonome établi ni de rôle d'indice d'émergence temporelle dans le programme actuel.

## Statuts de fin de journée

- Récupération/désinscription : `active`, arrêt logiciel local ; prochaine étape conditionnelle au matériel/réseau.
- Frustration temporelle : **`weakened`**, requalifiée comme score descriptif/optimiseur standard dans le programme actuel.
- Factorisation : `active` mais bornée ; reste triple robuste, H4 non soutenue.
- Orientation compositionnelle : `weakened`, P1/P2 clos.
- Distinctions compatibles : `suspended`.
- Holonomie S3 : affaiblie, fil `P_I` arrêté.

## Clôture

Aucun résultat du jour n'établit une nouvelle physique, un temps émergent ou un objet physique nouveau. Les résultats négatifs et absorptions standards sont conservés sans ajustement rétroactif.

À la reprise, refaire une allocation par valeur d'information au lieu de prolonger automatiquement la dernière branche. Priorités conditionnelles : vrai banc distribué pour récupération/désinscription ; quotient factoriel préenregistré ; complexe compatible seulement avec relation non injectée et contrôle apparié définis avant calcul.
