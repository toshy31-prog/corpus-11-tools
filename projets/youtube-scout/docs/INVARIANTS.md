# Invariants
- relationCount est descriptif, pas un score de priorité ;
- normalized(label) sert à la recherche, pas à l'identité ;
- équivalence seulement via relation explicite ou externalId partagé ;
- même nom sans preuve => choix distincts ;
- recherche : exact > préfixe > occurrence ;
- aucune mutation du graphe.
