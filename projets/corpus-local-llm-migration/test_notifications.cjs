const fs=require('fs'),vm=require('vm'),assert=require('assert');const src=fs.readFileSync(__dirname+'/portal/app.js','utf8');let count=0;function Notification(){count++}Notification.permission='granted';const c={generalSettings:{notifications:'background'},document:{visibilityState:'hidden'},Notification,window:{Notification}};vm.createContext(c);vm.runInContext(src.slice(src.indexOf('function notifyCorpusCompletion('),src.indexOf('function launchCorpusConfetti(')),c);const notify=c.notifyCorpusCompletion,m=id=>[{info:{id,role:'assistant',time:{completed:1},finish:'stop'}}];const s={id:'x',ready:false};notify(s,m('a'),{});assert.equal(count,0);s.ready=true;notify(s,m('a'),{});assert.equal(count,0);notify(s,m('b'),{});assert.equal(count,1);notify(s,m('b'),{});assert.equal(count,1);c.document.visibilityState='visible';notify(s,m('c'),{});assert.equal(count,1);console.log('Historique silencieux, dédoublonnage et arrière-plan : OK');
