#!/usr/bin/env python3
"""Repo-local entrypoint for the Corpus control plane."""
from pathlib import Path
import runpy
import sys

ROOT=Path(__file__).resolve().parents[1]
TARGET=ROOT/"projets/corpus-local-llm-migration/corpus_control.py"
sys.path.insert(0,str(TARGET.parent))
runpy.run_path(str(TARGET),run_name="__main__")
