# Procédure Expansion puis Audit

Source primaire : `archives/legacy/Atlas_3_0_final.zip`, passes `Expansion` et `Audit`.

Procédure candidate, invocation implicite désactivée : développer plusieurs mécanismes indépendants avant compression, puis auditer sans substituer une génération issue du corpus. Elle complète `explore-first` seulement quand la séparation des passes change le résultat.
