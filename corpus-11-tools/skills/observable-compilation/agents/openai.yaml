interface:
  display_name: "Observable Compilation"
  short_description: "Translate abstract claims into concrete observables, channels, windows, thresholds, and discriminating outcomes without treating the schema as proof."
  default_prompt: "Apply observable-compilation to this request."
policy:
  allow_implicit_invocation: true
