---
name: corpus-context-library
description: Consult the two long-form corpus works as contextual source material when the user explicitly asks to use, study, compare, summarize, or ground work in those documents. This library is not the 11.x execution graph and must not silently seed unrelated analysis or inédit fiction.
---

Use only when the user explicitly asks to work from the corpus books or when another active skill explicitly needs them as source context.

Preserve source framing and provenance. Do not treat either work as a neutral or exhaustive view. Do not replace the 11.x operational graph with these texts.

For inédit fiction, these documents are a field of exclusion unless the user explicitly asks for adaptation or reuse.
