interface:
  display_name: "Corpus Context Library"
  short_description: "Explicit access to the two long-form corpus works"
  default_prompt: "Use the corpus context library for this task."
policy:
  allow_implicit_invocation: false
