interface:
  display_name: "External Fiction Generation"
  short_description: "Generate inédit fiction independently, then audit it"
  default_prompt: "Create an inédit fiction using independent mechanism generation."
policy:
  allow_implicit_invocation: true
