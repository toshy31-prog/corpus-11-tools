interface:
  display_name: "Extraction Mapping"
  short_description: "Map extraction circuits, beneficiaries, carriers, dependencies, veto points, and costs. Use for class, resources, platforms, institutions, labor, and structural extraction."
  default_prompt: "Apply extraction-mapping to this request."
policy:
  allow_implicit_invocation: true
