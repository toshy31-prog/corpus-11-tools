interface:
  display_name: "Translation Risk Assessment"
  short_description: "Assess loss, semantic drift, status change, hierarchy, and pivot-language effects introduced by translation."
  default_prompt: "Apply translation-risk-assessment to this request."
policy:
  allow_implicit_invocation: true
